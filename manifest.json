{
    "applicationName": "Network deployment",
    "contactEmail": "gianni@ebi.ac.uk",
    "about": "Deploy a private network",
    "version": "0.5",
    "cloudProviders": [{
            "cloudProvider": "AWS",
            "path": "aws"
        },
        {
            "cloudProvider": "GCP",
            "path": "gcp",
            "inputs": ["project", "region"]
        },
        {
            "cloudProvider": "OSTACK",
            "path": "ostack"
        }
    ],
    "inputs": ["network_name"],
    "outputs": ["network_id", "network_name"]
}
